A rough attempt to make an HTML5 stereogram generator.  

My first stab at using git (have worked with CVS and SVN before, but never managing my own repo).  Also my first recreational programming project in several years.  Forgive the messy code…